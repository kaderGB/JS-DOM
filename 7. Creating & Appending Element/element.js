const h1 = document.createElement("h1"); //CreateElement to create a new element
h1.textContent = "hustler";
console.log(h1);
h1.classList.add("text");
const body = document.body;
body.appendChild(h1);  //AppendChild to insert the child inside the body


//selector.insertBefore()
//selector.insertAdjancantElement("beforeBegin",think to select)
//selector.insertAdjancantElement("afterbegin",think to select)
//selector.insertAdjancantElement(beforeend,think to select)
//selector.insertAdjancantElement(afterend,think to select)


//removeChild
